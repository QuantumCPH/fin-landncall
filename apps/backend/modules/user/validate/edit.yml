fields:
  user{name}:
    required:
      msg:              The user name cannot be left blank.
  user{email}:
    required:
      msg:              The user email cannot be left blank.
  user{password}:
    required:
      msg:              The user password cannot be left blank.
  user{role_id}:
    required:
      msg:              The user role cannot be left blank.

