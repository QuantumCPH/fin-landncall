generator:
  class:              sfPropelAdminGenerator
  param:
    model_class:      AgentUser
    theme:            default
    list:
      display: [id, username, password, agent_company, status]
      object_actions:
        _delete: ~
